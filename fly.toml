app = "alhaq-backend"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  cpu_kind = "shared"
  cpus = 2
  memory_mb = 512

[deploy]
  strategy = "rolling"

[processes]
  app = "python start_celery_only.py 2"
